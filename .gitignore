# Design-tool source files — used to edit the site in the design canvas,
# not needed to serve the live page (index.html is the self-contained build).
*.dc.html
support.js

# Original bundler export — duplicated byte-for-byte as index.html, which is
# what GitHub Pages actually serves.
FirestormFMD Site.html

# Internal preview thumbnail generated by the design tool.
.thumbnail

# Draft / duplicate images not referenced anywhere in index.html.
screenshots/
uploads/
